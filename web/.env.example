# Domain MAX 前端环境变量配置
# 适用于前后端分离架构（Cloudflare Pages + VPS API）

# ==============================================
# CLOUDFLARE PAGES 生产环境配置
# ==============================================

# API 基础URL（指向你的VPS API服务）
VITE_API_BASE_URL=https://api.yourdomain.com

# 后端域名（用于CORS等配置）
VITE_BACKEND_DOMAIN=api.yourdomain.com

# 前端URL（Cloudflare Pages URL）
VITE_CF_PAGES_URL=https://your-project.pages.dev

# 构建环境
NODE_ENV=production

# 构建目标（针对Cloudflare Pages优化）
VITE_BUILD_TARGET=cloudflare

# 应用标题和描述
VITE_APP_TITLE=Domain MAX
VITE_APP_DESCRIPTION=域名与DNS管理平台

# ==============================================
# 本地开发环境配置示例
# ==============================================

# 开发环境API地址（本地后端）
# VITE_API_BASE_URL=http://localhost:8080
# VITE_BACKEND_DOMAIN=localhost:8080
# NODE_ENV=development

# ==============================================
# 预览环境配置示例
# ==============================================

# 预览环境API地址（可能是测试服务器）
# VITE_API_BASE_URL=https://api-staging.yourdomain.com
# VITE_BACKEND_DOMAIN=api-staging.yourdomain.com
# NODE_ENV=preview

# ==============================================
# 功能开关配置
# ==============================================

# 启用调试模式
VITE_DEBUG_MODE=false

# 启用API请求日志
VITE_LOG_API_REQUESTS=false

# 启用性能监控
VITE_ENABLE_ANALYTICS=true

# ==============================================
# CLOUDFLARE PAGES 环境变量设置说明
# ==============================================

# 在Cloudflare Pages控制台中设置以下环境变量：
# 
# 生产环境 (Production):
#   NODE_ENV: production
#   VITE_API_BASE_URL: https://api.yourdomain.com
#   VITE_BACKEND_DOMAIN: api.yourdomain.com
#   VITE_BUILD_TARGET: cloudflare
#
# 预览环境 (Preview):
#   NODE_ENV: preview  
#   VITE_API_BASE_URL: https://api-staging.yourdomain.com
#   VITE_BACKEND_DOMAIN: api-staging.yourdomain.com
#   VITE_BUILD_TARGET: cloudflare

# ==============================================
# 开发环境设置说明
# ==============================================

# 本地开发时，创建 .env.development 文件：
# VITE_API_BASE_URL=http://localhost:8080
# VITE_BACKEND_DOMAIN=localhost:8080
# NODE_ENV=development
# VITE_DEBUG_MODE=true
# VITE_LOG_API_REQUESTS=true